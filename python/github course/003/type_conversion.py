# 类型转换
a = 100
b = 12.345
c = 'hello world'
d = True
# 整数转成浮点数
print(float(a))
# 浮点数转成字符串
print(str(b))
# 字符串转成布尔型（有内容的字符串都会变成True）
print(bool(c))
# 布尔型转成整数（True转成1，False转成0）
print(int(d))
# 将整数变成对应的字符（97刚好对应字符表中的字母a）
print(chr(97))
# 将字符串转成整数
print(ord('a'))